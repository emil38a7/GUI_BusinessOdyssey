<UserControl x:Class="GUI_BusinessOdyssey.GUI.ViewAllTeamsController"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI_BusinessOdyssey.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="8*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Label Content="students"></Label>
        <Label Content="judges" Grid.Column="1" ></Label>
        <ListView Grid.Row="1" Margin="10" VerticalAlignment="Stretch" ItemsSource="{Binding SGroup}" Grid.RowSpan="2">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="25" DisplayMemberBinding="{Binding SGroupId}" />
                    <GridViewColumn Header="Name" Width="100" DisplayMemberBinding="{Binding SGroupName}" />
                    <GridViewColumn Header="Track" Width="100" DisplayMemberBinding="{Binding TrackId}" />
                    <GridViewColumn Header="Student" Width="100" DisplayMemberBinding="{Binding student}" />
                    <GridViewColumn DisplayMemberBinding="{Binding student.StudentName}">
                        <DataTemplate>
                            <ListView ItemsSource="{Binding student}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding student.StudentName}"/>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </DataTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <ListView Grid.Column="1" Margin="10" Grid.Row="2" VerticalAlignment="Stretch" ItemsSource="{Binding StudentList}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="25" DisplayMemberBinding="{Binding StudentID}" />
                    <GridViewColumn Header="Name" Width="100" DisplayMemberBinding="{Binding StudentName}" />
                    <GridViewColumn Header="School" Width="100" DisplayMemberBinding="{Binding StudentSchool}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
